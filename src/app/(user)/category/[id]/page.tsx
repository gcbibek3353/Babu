// "use client"
import React from 'react'
import { toast } from 'sonner'

const page = () => {
    // toast("toast message")
  return (
    <div>
      Category wise products 
    {/* <button onClick={()=>toast("toast message")}>click</button> */}
    </div>
  )
}

export default page
